"""Provider adapter modules for racing and odds APIs.

Use these modules to isolate provider-specific payload shapes from the rest of
the application. The generic HTTP adapter works when env-configured endpoints
already return the normalized structures consumed by the sync service.
"""
